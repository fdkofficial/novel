# Django
SECRET_KEY=django-insecure-dev-key-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,172f43c4-8000.euw.devtunnels.ms

# Backend URL (for media files)
BACKEND_URL=https://elitevisiongmbh.de

# Database (SQLite for development)
# Using default SQLite, no need to configure

# Redis (optional)
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=7

# Email (optional for development)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True

# Celery (optional)
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# Frontend URL
FRONTEND_URL=http://localhost:54999

# S3 (disabled for development)
USE_S3=False
